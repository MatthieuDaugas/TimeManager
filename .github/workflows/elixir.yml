name: actions
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    container:
      image: docker:latest

    steps:
    - uses: actions/checkout@v1

    - name: Install Docker    
      run: |
      apt-get install docker-compose 
      docker-compose build
