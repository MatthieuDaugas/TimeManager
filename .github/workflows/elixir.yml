name: actions
on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
        with:
          otp-version: 22.2.6
          elixir-version: 1.10.1
      
      - run: pwd 
      - run: dir 

