name: actions
on: [push]
defaults:  
  run:  
    working-directory: ./apiTimeManager
jobs:
  build:
    runs-on: ubuntu-latest
    
    container:
      image: docker:latest

    steps:
    - uses: actions/checkout@v1

    - name: Install Docker
      run: |
        docker-compose build
